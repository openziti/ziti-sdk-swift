name: CI
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
permissions:
  contents: write

jobs:
  build-tsdk:
    runs-on: macos-latest

    strategy:
      matrix:
        spec:
        - { name: 'build-iphoneos-arm64', toolchain: 'iOS-arm64' }
        - { name: 'build-iphonesimulator-x86_64', toolchain: 'iOS-x86_64' }
        - { name: 'build-macosx-arm64', toolchain: 'macOS-arm64' }
        - { name: 'build-macosx-x86_64' }

    steps:
    - name: Checkout Project
      uses: actions/checkout@v3
      with:
        submodules: true

    - uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '13.2.1'

    - name: ${{ matrix.spec.name }}
      env:
        TOOLCHAIN: ${{ matrix.spec.toolchain && format('../../toolchains/{0}.cmake', matrix.spec.toolchain) || '' }}
      run: |
        cmake -DCMAKE_BUILD_TYPE=Release -DMBEDTLS_FATAL_WARNINGS:BOOL=OFF -DEXCLUDE_PROGRAMS=ON -DCMAKE_TOOLCHAIN_FILE="${TOOLCHAIN}" -S ./deps/ziti-tunnel-sdk-c -B ./deps/ziti-tunnel-sdk-c/${{ matrix.spec.name }}
        cmake --build ./deps/ziti-tunnel-sdk-c/${{ matrix.spec.name }}
        tar -cvzf ${{ matrix.spec.name }}.tgz -C ./deps/ziti-tunnel-sdk-c ${{ matrix.spec.name }}

    - name: Upload Build
      uses: actions/upload-artifact@v3
      with:
        name: ${{ matrix.spec.name }}.tgz
        path: ${{ matrix.spec.name }}.tgz

  build-cziti:
    runs-on: macos-latest
    needs: [ build-tsdk ]

    steps:
    - name: Checkout Project
      uses: actions/checkout@v3
      with:
        submodules: true

    - uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '13.2.1'

    - name: Download Artifacts
      uses: actions/download-artifact@v3

    - name: Extract TSDK Builds
      run: |
        for f in build-*.tgz ; do mv ${f} ${f}.zip && unzip ${f}.zip ; done
        tar -xzvf build-iphoneos-arm64.tgz -C ./deps/ziti-tunnel-sdk-c
        tar -xzvf build-iphonesimulator-x86_64.tgz -C ./deps/ziti-tunnel-sdk-c
        tar -xzvf build-macosx-x86_64.tgz -C ./deps/ziti-tunnel-sdk-c
        tar -xzvf build-macosx-arm64.tgz -C ./deps/ziti-tunnel-sdk-c

    - name: Build CZiti
      run: |
        xcodebuild build -configuration Release -scheme CZiti-iOS -derivedDataPath ./DerivedData/CZiti -arch arm64 -sdk iphoneos
        xcodebuild build -configuration Release -scheme CZiti-iOS -derivedDataPath ./DerivedData/CZiti -arch x86_64 -sdk iphonesimulator
        xcodebuild build -configuration Release -scheme CZiti-macOS -derivedDataPath ./DerivedData/CZiti -arch x86_64 -arch arm64 ONLY_ACTIVE_ARCH=NO -sdk macosx 

    - name: Create Frameworks
      run: |
        ./make_dist.sh
        tar -cvzf CZiti-iOS.framework.tgz -C ./dist/iOS/Release/Pods/CZiti-iOS/ .
        tar -cvzf CZiti-macOS.framework.tgz -C ./dist/macOS/Release/Pods/CZiti-macOS/ .
        pushd ./dist
        zip -r ../CZiti.xcframework.zip CZiti.xcframework
        popd

    - name: Upload CZiti-iOS.framework
      uses: actions/upload-artifact@v3
      with:
        name: CZiti-iOS.framework
        path: CZiti-iOS.framework.tgz

    - name: Upload CZiti-macOS.framework
      uses: actions/upload-artifact@v3
      with:
        name: CZiti-macOS.framework
        path: CZiti-macOS.framework.tgz

    - name: Upload CZiti.xcframework
      uses: actions/upload-artifact@v3
      with:
        name: CZiti.xcframework
        path: CZiti.xcframework.zip

    - name: Gen Docs
      run: |
        gem install jazzy
        rm *.podspec
        jazzy --hide-documentation-coverage -x '-arch,x86_64,-scheme,CZiti-macOS,-sdk,macosx' -m CZiti
        tar -cvzf ./ziti-sdk-swift-docs.tgz -C ./docs .

    - name: Upload Docs
      uses: actions/upload-artifact@v3
      with:
        name: ziti-sdk-swift-docs
        path: ziti-sdk-swift-docs.tgz

  draft-release:
    runs-on: ubuntu-latest
    needs: [ build-cziti ]
    if: github.event_name == 'push' && github.ref  == 'refs/heads/main'
    steps:
    - name: Checkout Project
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Download Docs
      uses: actions/download-artifact@v3
      with:
        name: ziti-sdk-swift-docs

    - name: Download iOS Framework
      uses: actions/download-artifact@v3
      with:
        name: CZiti-iOS.framework

    - name: Download macOS Framework
      uses: actions/download-artifact@v3
      with:
        name: CZiti-macOS.framework

    - name: Download CZiti.xcframework
      uses: actions/download-artifact@v3
      with:
        name: CZiti.xcframework

    - name: Draft Release
      id: release_drafter
      uses: release-drafter/release-drafter@v5
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload Docs
      uses: shogo82148/actions-upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.release_drafter.outputs.upload_url }}
        asset_path: ./ziti-sdk-swift-docs.tgz
        asset_name: ziti-sdk-swift-docs.tgz
        asset_content_type: application/tgz
        overwrite: true

    - name: Upload iOS Framework
      uses: shogo82148/actions-upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.release_drafter.outputs.upload_url }}
        asset_path: ./CZiti-iOS.framework.tgz
        asset_name: CZiti-iOS.framework.tgz
        asset_content_type: application/tgz
        overwrite: true

    - name: Upload macOS Framework
      uses: shogo82148/actions-upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.release_drafter.outputs.upload_url }}
        asset_path: ./CZiti-macOS.framework.tgz
        asset_name: CZiti-macOS.framework.tgz
        asset_content_type: application/tgz
        overwrite: true

    - name: Upload CZiti.xcframework
      uses: shogo82148/actions-upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.release_drafter.outputs.upload_url }}
        asset_path: ./CZiti.xcframework.zip
        asset_name: CZiti.xcframework.zip
        asset_content_type: application/zip
        overwrite: true
